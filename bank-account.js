//obtenion datos login , calculo de cuentas , mostrar en pantalla js, actualizacion de cookes, fin de sesion, validar errores

function loadUserDara() {
    let user = getSession();
    //pinta datos en panatalla
    document.getElementById
}


function deposit () {
    if(checkExpiration()){
    //condiciones y reglas del monto del usuario
    }
}

function withdraw(){
    if (checkExpiration()){
        //suma del monto del usuario
    }
}